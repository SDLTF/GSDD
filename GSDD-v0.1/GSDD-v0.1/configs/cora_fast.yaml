experiment:
  name: gsdd_v01_cora_fast
  seed: 1027
  output_root: results
  device: auto

dataset:
  name: Cora
  root: data
  normalize_features: true

attack:
  enabled: true
  target_class: 0
  selection_method: dirty_label
  poison_count: 14
  trigger_size: 3
  trigger_feature_count: 32
  trigger_feature_value: 1.0
  stamp_victim_features: true
  test_victim_count: 80

model:
  hidden_dim: 96
  dropout: 0.5
  supervised_epochs: 120
  ssl_epochs: 120
  learning_rate: 0.01
  weight_decay: 0.0005
  patience: 40

spectral:
  num_bands: 4
  moment_orders: [2, 3, 4, 5]
  hutchinson_probes: 32
  epsilon: 1.0e-8
  layers: [1, 2]

detection:
  degree_bins: 2
  minimum_group_size: 8
  mad_epsilon: 1.0e-6
  topk_fraction: 0.12
  score_clip: 10.0

output:
  save_models: true
  make_plots: true
  verbose: true
