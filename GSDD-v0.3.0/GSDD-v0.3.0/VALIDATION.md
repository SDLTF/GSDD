# Validation

## Python validation

- `python -m py_compile` passed for modified Python modules
- Synthetic smoke test completed successfully
- Supervised GCN, DGI, spectral filters, raw transfer, level/shape decomposition, calibration, plots and CSV export all completed
- New output files `band_transfer_shape_layer1.png` and `band_transfer_shape_layer2.png` were generated
- `aggregate_scale_audit.py` completed on the smoke result

## Platform note

The Python path was tested in the Linux execution environment. The PowerShell wrappers use the previously validated Windows PowerShell 5.1-safe `cmd.exe` output-merging helper. Final Windows execution must be confirmed on the user's machine.
